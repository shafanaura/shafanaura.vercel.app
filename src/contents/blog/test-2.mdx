# Contoh Kode MDX dengan Elemen Pre

Berikut adalah contoh kode JavaScript yang ditampilkan di dalam elemen `<pre>`:

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`)
}

greet('John')
```

Terima kasih sudah membaca contoh ini.
